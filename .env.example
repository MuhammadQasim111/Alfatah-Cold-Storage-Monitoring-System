# Smart Cold Storage Monitoring System - Environment Variables
# Copy this file to .env.local and fill in your actual credentials

# =====================================================
# DATABASE CONFIGURATION (Neon Serverless Postgres)
# =====================================================
# Get this from: https://console.neon.tech/app/projects
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=postgresql://neondb_owner:your_password@ep-shiny-lab-15602457.us-east-2.aws.neon.tech/neondb?sslmode=require

# =====================================================
# MQTT BROKER CONFIGURATION (HiveMQ Cloud)
# =====================================================
# Backend MQTT Configuration (for Node.js scripts)
# Get these from: https://console.hivemq.cloud/clusters/[your-cluster]/access-management
HIVEMQ_HOST=your-cluster.s1.eu.hivemq.cloud
HIVEMQ_USER=your_username
HIVEMQ_PASS=your_password

# Frontend MQTT Configuration (for browser WebSocket connection)
# Format: wss://[HIVEMQ_HOST]:8884/mqtt
NEXT_PUBLIC_HIVEMQ_WSS_URL=wss://your-cluster.s1.eu.hivemq.cloud:8884/mqtt
NEXT_PUBLIC_HIVEMQ_USER=your_username
NEXT_PUBLIC_HIVEMQ_PASS=your_password

# =====================================================
# NOTES
# =====================================================
# 1. NEVER commit .env.local to version control (it's in .gitignore)
# 2. For production deployment (Vercel), add these as Environment Variables in the dashboard
# 3. NEXT_PUBLIC_* variables are exposed to the browser, so use separate credentials if needed
# 4. The DATABASE_URL should include ?sslmode=require for Neon
# 5. HiveMQ WebSocket port is 8884 (not 8883 which is for MQTT over TLS)
